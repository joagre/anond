A = Next thing to do

node_route_serv.erl
-------------------
A Integrate Patrik's psp code and authentication of path costs code
* Use many overlay addresses and pick new ones now and then
* Pick new peers now and then

node_route_jsonrpc_serv.erl
---------------------------
* Add an experimental anond.conf configurable to cloak experimental
  jsonrpc functions.

ssl_serv.erl
------------
* Implement it
* Should be a plugin replacement for lib/util/src/tcp_serv.erl and let
  ds_jsonrpc_serv.erl and node_route_jsonrpc_serv.erl use it

node_tun_serv.erl
-----------------
A Think and implement it
A Setup a test setup on trashgrid.net, i.e. nine overlay nodes without
  tun device and one with a tun device. Setup
  http://media.pragprog.com/titles/jaerlang/code/shout.erl on the tun
  device.
* Intercept DNS traffic. Forward DNS lookups to other overlay
  nodes. Forward lookups for the "anond" domain to the anond directory
  server (see below).

node_{recv,send}_serv.erl
-------------------------
A Add encrytion using NaCl
A Accumulate and send everything in 1024 bytes UDP packets. Send cells
  anyway after a configurable amount of time (pad to 1024 bytes).

ds_jsonrpc_serv.erl
-------------------
* Add an experimental anond.cond configurable to cloak experimental
  jsonrpc functions.

ds_serv.erl
-----------
* http://www.h-online.com/security/features/How-Skype-Co-get-round-firewalls-747197.html
* Use OA as public key (should we?)
* Use dets instead of ets (should we?)
  - Add anond.conf parameter to set directory for dets file.
  - Add anond.conf parameter to clear the dets file on each restart.
* Make publish_peer/1, unpublish_peer/1 and published_peers/1 spoof
  proof. Signatures?
* Add DNS functionality for overlay addresses. Make it spoof
  proof. Signatures? 

generic stuff
-------------
* Write documentation (Use LyX)
  - White paper
  - End-user (client and server users)
* Ask Per to contribute the delayed start patch to the Erlang distro.
* Add package management support for:
  - Ubuntu
  - Mac
  - Windows
* Setup a nice anond site
* Implement anond control panels for:
  - GTK
  - Mac
  - Windows
